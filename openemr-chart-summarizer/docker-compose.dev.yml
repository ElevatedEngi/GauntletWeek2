version: '3.8'

services:
  agent:
    build: ./agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./agent/src:/app/src
    environment:
      - RELOAD=true
    depends_on:
      - openemr
    networks:
      - openemr-network